This shell script is used for those who want open two wechat in one MAC
You can download this sh and then open the terminal then use next commands to do it
remember replace the path to your real path before you run it
IF you can understant the code ,you can change something like name even do it for other apps by yourself ,it's very easy.
   
   ```
    1chmod +x ~/Desktop/wechat_dual.sh
    2~/Desktop/doubleWechat.sh
    ```
